module.exports = {
    assets: ['./assets/fonts'],

    // dependencies: {
    //     // Conditionally exclude 'react-native-flipper' if NO_FLIPPER=1 is set in the environment
    //     ...(process.env.NO_FLIPPER === "1" ? { 'react-native-flipper': { platforms: { ios: null } } } : {}),
    // },
};